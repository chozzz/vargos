/**
 * Re-exported verbatim from vargos `lib/frontmatter.ts` — no local copy. The
 * console parses cron/persona frontmatter exactly the way the daemon does.
 */

export {
  parseFrontmatter,
  serializeFrontmatter,
  type FrontmatterResult,
} from "@vargos/lib/frontmatter";
